<div class="container d-flex p-3 text-center flex-column revisor-edit-page-empty">
    <div class="px-3 my-auto">
        <h1 class="d-md-display-3">{{__('ui.ops')}}</h1>
        <p class="lead d-md-display-5">{{__('ui.cosaFare')}}</p>
        <p class="lead">
        <a href="{{route('home')}}" class="btn btn-lg btn-secondary fw-bold border-white bg-warning text-dark mx-4 mb-3">Torna alla Home</a>
        <a href="{{route('contact')}}" class="btn btn-lg btn-secondary fw-bold border-white bg-danger text-dark mx-4 mb-3">Contatta un membro dello staff</a>
        </p>
    </div>
</div>
